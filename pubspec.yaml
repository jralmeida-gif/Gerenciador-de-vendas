name: gestor_vendas
description: Gestor de Vendas - controle de vendas, portabilidade, prospecção e metas.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: 6.1.5+1
  hive: 2.2.3
  hive_flutter: 1.1.0
  shared_preferences: 2.5.3
  intl: ^0.20.2
  pdf: ^3.11.1
  printing: ^5.13.4
  file_picker: ^8.1.7
  fl_chart: ^0.69.2
  uuid: ^4.5.1
  http: ^1.3.0
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
